---
phase: 03-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jazlab-hub/src/components/ParticleBackground.tsx
  - jazlab-hub/src/components/MotionWrapper.tsx
  - jazlab-hub/src/components/AnimatedBentoGrid.tsx
  - jazlab-hub/src/app/page.tsx
  - jazlab-hub/tests/particle-background.test.ts
  - jazlab-hub/tests/motion-wrapper.test.ts
  - jazlab-hub/tests/homepage.test.ts
autonomous: true
requirements: [BRAND-01, SHOW-01, SHOW-03, SHOW-04, SEO-03]

must_haves:
  truths:
    - "Visitor sees the JazLab logo, a gradient headline, and a subheading within 3 seconds of landing"
    - "Visitor sees floating cosmic particles drifting across the deep space background"
    - "Visitor sees all 3 experiment cards in an animated bento grid that reveals on scroll"
    - "Homepage layout is responsive across mobile, tablet, and desktop breakpoints"
  artifacts:
    - path: "jazlab-hub/src/components/ParticleBackground.tsx"
      provides: "Canvas particle animation with cosmic floating effects"
      contains: "use client"
    - path: "jazlab-hub/src/components/MotionWrapper.tsx"
      provides: "Thin client wrapper for motion scroll-reveal animations"
      contains: "use client"
    - path: "jazlab-hub/src/components/AnimatedBentoGrid.tsx"
      provides: "Client component wrapping BentoGrid cards with staggered whileInView animation"
      contains: "use client"
    - path: "jazlab-hub/src/app/page.tsx"
      provides: "Full homepage with hero section, particle background, and animated experiment grid"
      exports: ["default"]
  key_links:
    - from: "jazlab-hub/src/app/page.tsx"
      to: "jazlab-hub/src/components/ParticleBackground.tsx"
      via: "import and render as fixed background layer"
      pattern: "ParticleBackground"
    - from: "jazlab-hub/src/app/page.tsx"
      to: "jazlab-hub/src/components/AnimatedBentoGrid.tsx"
      via: "import and render in experiments section"
      pattern: "AnimatedBentoGrid"
    - from: "jazlab-hub/src/components/AnimatedBentoGrid.tsx"
      to: "jazlab-hub/src/components/ExperimentCard.tsx"
      via: "renders ExperimentCard inside motion.div wrappers"
      pattern: "ExperimentCard"
    - from: "jazlab-hub/src/app/page.tsx"
      to: "jazlab-hub/src/components/GradientHeading.tsx"
      via: "import for hero headline"
      pattern: "GradientHeading"
---

<objective>
Build the JazLab homepage with a cosmic particle background, hero section (logo + gradient headline + subheading), and animated bento grid of experiment cards with scroll-triggered reveal.

Purpose: This is the primary landing page visitors see. It must immediately communicate what JazLab is and showcase the three experiments with the cosmic laboratory aesthetic from the reference mockup.

Output: Fully realized homepage replacing the placeholder, plus three new client components (ParticleBackground, MotionWrapper, AnimatedBentoGrid) with passing tests.
</objective>

<execution_context>
@/Users/dbeacham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dbeacham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pages/03-RESEARCH.md
@.planning/phases/03-pages/03-VALIDATION.md
@.planning/phases/02-ui-system/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/types/experiments.ts:
```typescript
export type ExperimentStatus = "active" | "beta" | "coming-soon";

export interface ExperimentFeature {
  title: string;
  description: string;
  icon: string;
}

export interface Experiment {
  slug: string;
  name: string;
  description: string;
  status: ExperimentStatus;
  subdomainUrl: string;
  accentColor: string;
  features: ExperimentFeature[];
}
```

From src/lib/experiments.ts:
```typescript
export const experiments: Experiment[];
// 3 items: blockabye (beta), brickify (beta), sournal (coming-soon)
```

From src/components/GradientHeading.tsx:
```typescript
export function GradientHeading({ as, children, className }: {
  as?: "h1" | "h2" | "h3" | "h4";
  children: React.ReactNode;
  className?: string;
});
```

From src/components/ExperimentCard.tsx:
```typescript
export function ExperimentCard({ experiment, featured, className }: {
  experiment: Experiment;
  featured?: boolean;
  className?: string;
});
```

From src/components/BentoGrid.tsx:
```typescript
// Standalone data-driven component -- imports experiments internally
// Grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 auto-rows-[280px] gap-4
// First card featured (index === 0)
export function BentoGrid();
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/app/globals.css:
- body::before: fixed noise layer at z-index: 0
- body > *: position relative, z-index 1
- Brand tokens: --color-violet, --color-teal, --color-cyan, --color-sparkle
- Per-app: --color-blockabye, --color-brickify, --color-sournal
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install motion + create ParticleBackground, MotionWrapper, and AnimatedBentoGrid client components</name>
  <files>
    jazlab-hub/src/components/ParticleBackground.tsx,
    jazlab-hub/src/components/MotionWrapper.tsx,
    jazlab-hub/src/components/AnimatedBentoGrid.tsx,
    jazlab-hub/tests/particle-background.test.ts,
    jazlab-hub/tests/motion-wrapper.test.ts
  </files>
  <behavior>
    particle-background.test.ts:
    - ParticleBackground.tsx has "use client" directive
    - Contains useRef and useEffect imports from react
    - Contains requestAnimationFrame for animation loop
    - Contains cancelAnimationFrame for cleanup
    - Canvas element has className with "fixed" and "inset-0" and "pointer-events-none"
    - Contains aria-hidden="true" for accessibility
    - Contains window.addEventListener("resize") for responsive canvas sizing
    - Contains particle color references matching brand palette (rgba for teal 40,199,183 or white 242,244,248)
    - Reduces particle count on mobile via matchMedia or similar check

    motion-wrapper.test.ts:
    - MotionWrapper.tsx has "use client" directive
    - Imports motion from "motion/react"
    - Exports MotionWrapper function
    - Contains HTMLMotionProps type reference
    - Spreads motionProps onto motion.div (rest/spread pattern)

    AnimatedBentoGrid (tested in homepage.test.ts or standalone):
    - Has "use client" directive
    - Imports motion from "motion/react"
    - Imports ExperimentCard from @/components/ExperimentCard
    - Imports experiments from @/lib/experiments
    - Contains whileInView for scroll-triggered animation
    - Contains viewport with once: true
    - Contains initial with opacity: 0 (fade-in pattern)
    - Grid classes: grid-cols-1, md:grid-cols-2, lg:grid-cols-3
    - Featured card: index === 0 with lg:col-span-2
  </behavior>
  <action>
    1. Install motion package: `cd jazlab-hub && npm install motion` (use --legacy-peer-deps only if ERESOLVE error occurs per Pitfall 6).

    2. Create `src/components/ParticleBackground.tsx` as a "use client" component:
       - Full-viewport HTML5 Canvas with fixed position, inset-0, pointer-events-none, z-index 0 (via style prop, NOT className -- matches body::before stacking per Pitfall 1)
       - aria-hidden="true" on canvas element
       - useRef for canvas, useEffect for animation loop
       - PARTICLE_COUNT = 120 (desktop), reduce to 60 on mobile using window.matchMedia("(max-width: 768px)") inside useEffect
       - Particles: { x, y, z (depth 0-1), vx, vy } with gentle drift (0.15 max velocity)
       - Wrap edges when particles go off-screen
       - Radius: 0.5 + z * 1.5 (0.5px to 2px), opacity: 0.2 + z * 0.6 (20% to 80%)
       - Color: z > 0.7 ? rgba(40,199,183, opacity) [teal] : rgba(242,244,248, opacity) [near-white]
       - Add occasional sparkle particles (z > 0.9): use sparkle gold rgba(255,211,107, opacity) for ~5% of particles
       - requestAnimationFrame loop, cleanup with cancelAnimationFrame + removeEventListener on unmount
       - Resize handler that updates canvas.width/canvas.height on window resize

    3. Create `src/components/MotionWrapper.tsx` as a "use client" thin wrapper:
       - Import { motion, HTMLMotionProps } from "motion/react"
       - Import { cn } from "@/lib/utils"
       - Interface MotionWrapperProps extends HTMLMotionProps<"div"> with optional className
       - Renders motion.div with cn(className) and spreads ...motionProps
       - Passes children through

    4. Create `src/components/AnimatedBentoGrid.tsx` as a "use client" component:
       - Import { motion } from "motion/react"
       - Import { ExperimentCard } from "@/components/ExperimentCard"
       - Import { experiments } from "@/lib/experiments"
       - Render the same grid layout as BentoGrid (grid-cols-1 md:grid-cols-2 lg:grid-cols-3 auto-rows-[280px] gap-4)
       - Wrap each ExperimentCard in a motion.div with:
         - initial={{ opacity: 0, y: 32 }}
         - whileInView={{ opacity: 1, y: 0 }}
         - viewport={{ once: true, margin: "-5% 0px" }}
         - transition={{ duration: 0.5, delay: index * 0.12 }}
       - Featured card (index === 0): add className "md:col-span-1 lg:col-span-2" to the motion.div wrapper
       - Pass featured={index === 0} to ExperimentCard

    5. Write test files FIRST (RED), then create components (GREEN):
       - tests/particle-background.test.ts: file-read tests per behavior list above
       - tests/motion-wrapper.test.ts: file-read tests per behavior list above

    CRITICAL CONVENTIONS:
    - Only animate opacity, y, x, scale (GPU-composited). Never animate height, width, top, left.
    - Canvas style={{ zIndex: 0 }} (inline, not className) so it layers correctly with body::before noise.
    - app/page.tsx remains a Server Component -- all "use client" is isolated to these wrapper components.
    - Use Tailwind v4 syntax: bg-linear-to-r (NOT bg-gradient-to-r).
  </action>
  <verify>
    <automated>cd jazlab-hub && npm run test:run -- tests/particle-background.test.ts tests/motion-wrapper.test.ts</automated>
  </verify>
  <done>
    - motion package installed in package.json
    - ParticleBackground.tsx renders full-viewport canvas with floating cosmic particles (teal + white + occasional gold sparkle)
    - MotionWrapper.tsx provides generic motion.div wrapper for scroll-reveal in Server Component pages
    - AnimatedBentoGrid.tsx renders all 3 experiment cards with staggered scroll-triggered fade-in
    - All particle-background and motion-wrapper tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build homepage with cosmic hero section, particle background, and animated experiment grid</name>
  <files>
    jazlab-hub/src/app/page.tsx,
    jazlab-hub/tests/homepage.test.ts
  </files>
  <behavior>
    homepage.test.ts:
    - page.tsx imports ParticleBackground from @/components/ParticleBackground
    - page.tsx imports AnimatedBentoGrid from @/components/AnimatedBentoGrid
    - page.tsx imports GradientHeading from @/components/GradientHeading
    - Contains img element with src="/jazlab-logo.svg" (hero logo)
    - Contains the headline text "A laboratory for software experiments" (or similar value proposition)
    - Contains "Experiments currently running" or "experiments" section identifier
    - Contains id="experiments" anchor for the nav link from SiteHeader (/#experiments)
    - Contains responsive classes: uses px-4 or similar padding, max-w-* container constraint
    - Does NOT contain "use client" directive (page.tsx stays Server Component)
    - Uses GradientHeading component (as="h1" or similar)
    - Homepage renders MotionWrapper for hero entrance animation (imports MotionWrapper)
  </behavior>
  <action>
    1. Write tests/homepage.test.ts FIRST (RED) with file-read assertions per behavior list above.

    2. Replace `src/app/page.tsx` placeholder with full homepage layout. Page remains a Server Component (no "use client"). Structure:

       a. ParticleBackground component rendered at the top (fixed canvas, behind everything)

       b. Hero section (centered, full viewport height minus header):
          - Container: max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center
          - Vertical flex layout centered
          - MotionWrapper wrapping hero content with initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, ease: "easeOut" }}
          - JazLab logo: <img src="/jazlab-logo.svg" alt="JazLab" width={300} height={80} className="mx-auto h-16 sm:h-20 w-auto mb-8" />
          - Headline: <GradientHeading as="h1" className="text-4xl sm:text-5xl lg:text-6xl mb-4">A laboratory for software experiments</GradientHeading>
          - Subheading: <p className="text-lg sm:text-xl text-text-secondary max-w-2xl mx-auto">Where ideas become experiments and experiments become products.</p>

       c. Experiments section:
          - Section with id="experiments" (anchor target from SiteHeader nav link /#experiments)
          - Container: max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-16 sm:py-24
          - Section heading: <GradientHeading as="h2" className="text-2xl sm:text-3xl mb-8 text-center">Experiments currently running</GradientHeading>
          - AnimatedBentoGrid component (replaces static BentoGrid)

       d. Bottom spacing: pb-16 or similar to prevent footer crowding

    3. Run tests to confirm all pass (GREEN).

    CRITICAL LAYOUT NOTES:
    - The hero section should feel like the reference mockup: cosmic background visible through content, centered text, cards below
    - ParticleBackground is behind everything via fixed position + z-index: 0
    - All page content has position: relative + z-index: 1 already from globals.css body > * rule
    - Do NOT add a perspective grid at the bottom (defer that visual flourish -- keep scope tight)
    - The <main> wrapper in layout.tsx already provides flex-1 -- do not duplicate <main> in page.tsx (remove the existing <main> wrapper from the placeholder)
  </action>
  <verify>
    <automated>cd jazlab-hub && npm run test:run -- tests/homepage.test.ts && npm run build</automated>
  </verify>
  <done>
    - Homepage shows JazLab logo, gradient headline "A laboratory for software experiments", and subheading
    - Cosmic particle background floats behind all content
    - Three experiment cards animate in with staggered scroll-reveal
    - id="experiments" anchor exists for SiteHeader /#experiments link
    - Page remains a Server Component (no "use client" in page.tsx)
    - npm run build passes with homepage as static (circle symbol, not lambda)
    - All homepage.test.ts assertions pass
  </done>
</task>

</tasks>

<verification>
1. `cd jazlab-hub && npm run test:run` -- all tests pass (existing 68 + new homepage/particle/motion tests)
2. `cd jazlab-hub && npm run build` -- exits 0, homepage renders as static (circle), no lambda pages
3. Homepage at localhost:3000 shows: logo, gradient headline, particle background, 3 animated experiment cards
</verification>

<success_criteria>
- ParticleBackground renders cosmic floating particles (teal, white, gold sparkle) on full-viewport canvas
- MotionWrapper enables scroll-reveal animations from Server Component pages
- AnimatedBentoGrid shows 3 experiment cards with staggered fade-in on scroll
- Homepage hero displays JazLab logo + gradient headline + subheading matching reference mockup aesthetic
- All new tests pass; existing 68 tests still pass; build remains fully static
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages/03-01-SUMMARY.md`
</output>
