---
phase: 02-ui-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jazlab-hub/src/components/ui/badge.tsx
  - jazlab-hub/src/components/ui/card.tsx
  - jazlab-hub/src/components/GradientHeading.tsx
  - jazlab-hub/src/components/ExperimentBadge.tsx
  - jazlab-hub/src/components/SiteHeader.tsx
  - jazlab-hub/src/components/SiteFooter.tsx
  - jazlab-hub/tests/gradient-heading.test.ts
  - jazlab-hub/tests/experiment-badge.test.ts
  - jazlab-hub/tests/site-header.test.ts
  - jazlab-hub/tests/site-footer.test.ts
autonomous: true
requirements: [BRAND-02, BRAND-03, BRAND-05, BRAND-06]

must_haves:
  truths:
    - "A GradientHeading component renders headings with violet-to-teal gradient text using Tailwind v4 bg-linear-to-r"
    - "An ExperimentBadge component maps each ExperimentStatus (active, beta, coming-soon) to a labeled, color-coded badge"
    - "A sticky navigation header links to the hub and all 3 experiment app subdomains using lab terminology"
    - "A footer displays experiment links, GitHub social link, and contact email on every page"
  artifacts:
    - path: "jazlab-hub/src/components/GradientHeading.tsx"
      provides: "Reusable gradient heading with bg-linear-to-r from-violet to-teal"
      contains: "bg-linear-to-r"
    - path: "jazlab-hub/src/components/ExperimentBadge.tsx"
      provides: "Status badge mapping ExperimentStatus to labeled badge"
      contains: "STATUS_CONFIG"
    - path: "jazlab-hub/src/components/SiteHeader.tsx"
      provides: "Sticky navigation header with experiment links"
      contains: "sticky top-0"
    - path: "jazlab-hub/src/components/SiteFooter.tsx"
      provides: "Footer with experiment links, social links, contact"
      contains: "experiments.map"
    - path: "jazlab-hub/src/components/ui/badge.tsx"
      provides: "shadcn Badge primitive"
    - path: "jazlab-hub/src/components/ui/card.tsx"
      provides: "shadcn Card primitive"
  key_links:
    - from: "jazlab-hub/src/components/ExperimentBadge.tsx"
      to: "jazlab-hub/src/components/ui/badge.tsx"
      via: "import Badge"
      pattern: "import.*Badge.*from.*@/components/ui/badge"
    - from: "jazlab-hub/src/components/ExperimentBadge.tsx"
      to: "jazlab-hub/src/types/experiments.ts"
      via: "import ExperimentStatus type"
      pattern: "import.*ExperimentStatus.*from.*@/types/experiments"
    - from: "jazlab-hub/src/components/SiteHeader.tsx"
      to: "jazlab-hub/src/lib/experiments.ts"
      via: "import experiments array for nav links"
      pattern: "import.*experiments.*from.*@/lib/experiments"
    - from: "jazlab-hub/src/components/SiteFooter.tsx"
      to: "jazlab-hub/src/lib/experiments.ts"
      via: "import experiments array for footer links"
      pattern: "import.*experiments.*from.*@/lib/experiments"
---

<objective>
Build the four leaf-level shared UI components for JazLab: GradientHeading, ExperimentBadge, SiteHeader, and SiteFooter. Install the shadcn Badge and Card primitives that downstream components depend on.

Purpose: These components are the building blocks for Phase 2's composite components (ExperimentCard, BentoGrid) and Phase 3's page assembly. Each is validated in isolation with Vitest file-read tests before any page consumes them.

Output: Six component files (4 custom + 2 shadcn), four test files, all tests green.
</objective>

<execution_context>
@/Users/dbeacham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dbeacham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-system/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from Phase 1 codebase. -->

From jazlab-hub/src/types/experiments.ts:
```typescript
export type ExperimentStatus = "active" | "beta" | "coming-soon";

export interface ExperimentFeature {
  title: string;
  description: string;
  icon: string;
}

export interface Experiment {
  slug: string;
  name: string;
  description: string;
  status: ExperimentStatus;
  subdomainUrl: string;
  accentColor: string;
  features: ExperimentFeature[];
}
```

From jazlab-hub/src/lib/experiments.ts:
```typescript
import type { Experiment } from "@/types/experiments";
export const experiments: Experiment[] = [
  { slug: "blockabye", name: "BlockAbye", status: "beta", ... },
  { slug: "brickify", name: "Brickify", status: "beta", ... },
  { slug: "sournal", name: "Sournal", status: "coming-soon", ... },
];
```

From jazlab-hub/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

Brand tokens available in globals.css @theme:
- Colors: violet (#5B3DF5), teal (#28C7B7), cyan (#00D4FF), sparkle (#FFD36B)
- Surfaces: surface (#0F1117), surface-raised (#1A1E2A), surface-overlay (#242938), border (#32384A)
- Text: text-primary (#F2F4F8), text-secondary (#A4A9B6), text-muted (#6C7385)
- Fonts: font-display (Space Grotesk), font-body (Inter), font-mono (JetBrains Mono)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn primitives and build GradientHeading + ExperimentBadge</name>
  <files>
    jazlab-hub/src/components/ui/badge.tsx,
    jazlab-hub/src/components/ui/card.tsx,
    jazlab-hub/src/components/GradientHeading.tsx,
    jazlab-hub/src/components/ExperimentBadge.tsx,
    jazlab-hub/tests/gradient-heading.test.ts,
    jazlab-hub/tests/experiment-badge.test.ts
  </files>
  <action>
    **Step 1: Install shadcn Badge and Card primitives.**
    Run from `jazlab-hub/` directory:
    ```bash
    npx shadcn@latest add badge --yes
    npx shadcn@latest add card --yes
    ```
    This creates `src/components/ui/badge.tsx` and `src/components/ui/card.tsx`. Do NOT hand-edit these files.

    **Step 2: Create GradientHeading component** at `src/components/GradientHeading.tsx`.
    - Pure Server Component (no "use client" directive)
    - Props: `as?: "h1" | "h2" | "h3" | "h4"` (default "h2"), `children: React.ReactNode`, `className?: string`
    - Apply classes via `cn()`: `bg-linear-to-r from-violet to-teal bg-clip-text text-transparent inline-block font-display font-extrabold`
    - CRITICAL: Use `bg-linear-to-r` (Tailwind v4 syntax). Do NOT use `bg-gradient-to-r` (v3 name, silently produces nothing in v4).
    - Include `inline-block` so gradient spans end-to-end across text width.
    - Render the dynamic Tag element via the `as` prop pattern.
    - Follow exact implementation from RESEARCH.md Pattern 1.

    **Step 3: Create ExperimentBadge component** at `src/components/ExperimentBadge.tsx`.
    - Pure Server Component (no "use client" directive)
    - Import `Badge` from `@/components/ui/badge` and `ExperimentStatus` from `@/types/experiments`
    - Define `STATUS_CONFIG: Record<ExperimentStatus, { label: string; className: string }>` with:
      - `active`: label "Active", className `bg-teal/10 text-teal border-teal/20`
      - `beta`: label "Beta", className `bg-violet/10 text-violet border-violet/20`
      - `coming-soon`: label "Coming Soon", className `bg-surface-overlay text-text-muted border-border`
    - Props: `status: ExperimentStatus`, `className?: string`
    - Render `<Badge variant="outline">` with `cn("font-mono text-xs uppercase tracking-wider", config.className, className)`
    - Badge displays `config.label` as children text
    - This is the single source of truth for status terminology (BRAND-05).
    - Follow exact implementation from RESEARCH.md Pattern 2.

    **Step 4: Create test files** using the Phase 1 file-read pattern (fs.readFileSync, string assertions).

    `tests/gradient-heading.test.ts`:
    - Read `src/components/GradientHeading.tsx` as string
    - Assert contains `bg-linear-to-r` (v4 gradient syntax)
    - Assert does NOT contain `bg-gradient-to-r` (v3 anti-pattern)
    - Assert contains `from-violet` and `to-teal` (brand tokens)
    - Assert contains `bg-clip-text` and `text-transparent`
    - Assert contains `inline-block`
    - Assert contains `font-display` and `font-extrabold`

    `tests/experiment-badge.test.ts`:
    - Read `src/components/ExperimentBadge.tsx` as string
    - Assert contains all 3 status labels: "Active", "Beta", "Coming Soon"
    - Assert contains status keys: `active`, `beta`, `coming-soon`
    - Assert contains brand color classes: `text-teal`, `text-violet`, `text-text-muted`
    - Assert imports from `@/components/ui/badge`
    - Assert imports `ExperimentStatus` from `@/types/experiments`
    - Assert contains `font-mono` and `uppercase` for lab-styled badge text
  </action>
  <verify>
    <automated>cd /Users/dbeacham/Development/jazlab-hub && npm run test:run -- tests/gradient-heading.test.ts tests/experiment-badge.test.ts</automated>
  </verify>
  <done>
    shadcn Badge and Card primitives installed at src/components/ui/.
    GradientHeading renders h2-h4 tags with violet-to-teal gradient using Tailwind v4 bg-linear-to-r syntax.
    ExperimentBadge maps all 3 ExperimentStatus values to labeled, color-coded badges.
    All gradient-heading and experiment-badge tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build SiteHeader and SiteFooter with lab terminology</name>
  <files>
    jazlab-hub/src/components/SiteHeader.tsx,
    jazlab-hub/src/components/SiteFooter.tsx,
    jazlab-hub/tests/site-header.test.ts,
    jazlab-hub/tests/site-footer.test.ts
  </files>
  <action>
    **Step 1: Create SiteHeader** at `src/components/SiteHeader.tsx`.
    - Pure Server Component (no "use client" — no active link highlighting in Phase 2 per RESEARCH.md recommendation). If active link is needed, Phase 3 will extract a thin ActiveLink client leaf.
    - Import `Link` from `next/link` and `{ experiments }` from `@/lib/experiments`
    - Sticky header: `sticky top-0 z-50 bg-surface/80 backdrop-blur-md border-b border-border`
    - Inner nav: `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between`
    - Left: JazLab logo as `<Link href="/">` with `font-display font-bold text-text-primary hover:text-violet transition-colors`
    - Right: `<ul>` with `hidden md:flex items-center gap-6 text-sm text-text-secondary`
      - "Experiments" link pointing to `/#experiments` (lab terminology, BRAND-05)
      - Map over `experiments` array to create per-app links using `exp.subdomainUrl` with `target="_blank" rel="noopener noreferrer"`
      - Each link: `hover:text-text-primary transition-colors`
    - Mobile: nav links hidden on mobile (`hidden md:flex`). Mobile hamburger menu deferred to Phase 3.
    - Follow exact implementation from RESEARCH.md Pattern 5.

    **Step 2: Create SiteFooter** at `src/components/SiteFooter.tsx`.
    - Pure Server Component (no "use client")
    - Import `Link` from `next/link`, `{ experiments }` from `@/lib/experiments`, `{ Github }` from `lucide-react`
    - Outer: `<footer className="border-t border-border mt-auto">`
    - Inner: `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12`
    - 3-column grid (`grid grid-cols-1 md:grid-cols-3 gap-8`):
      - Column 1 (Brand): "JazLab" in `font-display font-bold text-text-primary`, tagline "A laboratory for software experiments." in `text-sm text-text-muted`
      - Column 2 (Experiments — BRAND-05 lab terminology): Section label "Experiments" in `text-xs font-mono uppercase tracking-wider text-text-muted mb-3`, then `experiments.map()` rendering each as `<Link>` to `exp.subdomainUrl` with `target="_blank"`
      - Column 3 (Contact): Section label "Contact", GitHub link to `https://github.com/daniel-beacham` with `<Github className="h-4 w-4" />` icon, email link to `mailto:hello@jazlab.llc`
    - Bottom bar: `mt-8 pt-8 border-t border-border text-xs text-text-muted` with copyright `{new Date().getFullYear()} JazLab. All experiments reserved.` (lab terminology in copyright)
    - Follow exact implementation from RESEARCH.md Pattern 6.

    **Step 3: Create test files** using Phase 1 file-read pattern.

    `tests/site-header.test.ts`:
    - Read `src/components/SiteHeader.tsx` as string
    - Assert contains `sticky` and `top-0` (sticky positioning)
    - Assert contains `z-50` (above noise layer)
    - Assert contains `backdrop-blur` (glass effect)
    - Assert contains `href="/"` (home link)
    - Assert contains `/#experiments` (experiments section link — lab terminology)
    - Assert contains import from `@/lib/experiments` (data-driven nav links)
    - Assert contains `target="_blank"` (external experiment links)
    - Assert contains `hidden md:flex` or `hidden` and `md:flex` (responsive nav hide on mobile)
    - Assert does NOT contain `"use client"` (must be Server Component)

    `tests/site-footer.test.ts`:
    - Read `src/components/SiteFooter.tsx` as string
    - Assert contains `border-t` (top border separator)
    - Assert contains import from `@/lib/experiments` (data-driven experiment links)
    - Assert contains `github.com/daniel-beacham` (social link)
    - Assert contains `hello@jazlab.llc` (contact email)
    - Assert contains `Experiments` (lab terminology section header)
    - Assert contains `getFullYear` (dynamic copyright year)
    - Assert contains `Github` import from `lucide-react`
    - Assert does NOT contain `"use client"` (must be Server Component)
  </action>
  <verify>
    <automated>cd /Users/dbeacham/Development/jazlab-hub && npm run test:run -- tests/site-header.test.ts tests/site-footer.test.ts</automated>
  </verify>
  <done>
    SiteHeader renders a sticky nav bar with JazLab logo, "Experiments" anchor link, and all 3 experiment subdomain links.
    SiteFooter renders a 3-column footer with brand info, experiment links, GitHub social link, and contact email.
    Both use lab terminology ("Experiments", "experiments reserved").
    Both are pure Server Components (no "use client").
    All site-header and site-footer tests pass.
  </done>
</task>

</tasks>

<verification>
All 4 new test files pass: `cd /Users/dbeacham/Development/jazlab-hub && npm run test:run`
Full test suite (Phase 1 + Phase 2 tests) passes with zero failures.
shadcn Badge and Card files exist at src/components/ui/.
No component contains "use client" directive.
`npm run build` still produces all static output (no new lambda pages).
</verification>

<success_criteria>
- GradientHeading uses `bg-linear-to-r from-violet to-teal bg-clip-text text-transparent` (Tailwind v4 syntax)
- ExperimentBadge maps active->Active, beta->Beta, coming-soon->Coming Soon with brand colors
- SiteHeader is sticky with z-50, links to home and all 3 experiments, uses lab terminology
- SiteFooter has 3-column layout with experiment links, GitHub, and contact email
- All 4 test files pass (gradient-heading, experiment-badge, site-header, site-footer)
- Full test suite green, build still static
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-system/02-01-SUMMARY.md`
</output>
