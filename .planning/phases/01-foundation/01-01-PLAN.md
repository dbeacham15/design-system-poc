---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jazlab-hub/package.json
  - jazlab-hub/tsconfig.json
  - jazlab-hub/next.config.ts
  - jazlab-hub/postcss.config.mjs
  - jazlab-hub/components.json
  - jazlab-hub/src/app/globals.css
  - jazlab-hub/src/app/layout.tsx
  - jazlab-hub/src/app/page.tsx
  - jazlab-hub/src/lib/utils.ts
autonomous: true
requirements:
  - BRAND-04
  - BRAND-07

must_haves:
  truths:
    - "npm run dev starts and renders a dark background (#0F1117) with zero white flash on load"
    - "Brand color utilities (bg-violet, text-teal, bg-surface-raised, etc.) generate from Tailwind @theme tokens"
    - "Three fonts (Space Grotesk display, Inter body, JetBrains Mono mono) load via next/font with CSS variables"
    - "Dark background shows subtle noise/grain texture at 6% opacity via body::before pseudo-element"
    - "shadcn/ui CSS variables (--primary, --background, --card, etc.) are wired to JazLab brand values"
    - "Gradient tokens (--gradient-brand, --gradient-accent) are defined as CSS custom properties"
  artifacts:
    - path: "jazlab-hub/src/app/globals.css"
      provides: "Brand tokens via @theme, shadcn bridge via @theme inline, noise layer via body::before"
      contains: "@theme"
    - path: "jazlab-hub/src/app/layout.tsx"
      provides: "Three-font setup, dark class on html, metadata with NEXT_PUBLIC_SITE_URL"
      exports: ["RootLayout", "metadata"]
    - path: "jazlab-hub/src/app/page.tsx"
      provides: "Minimal homepage Server Component (placeholder for Phase 3)"
    - path: "jazlab-hub/components.json"
      provides: "shadcn/ui configuration wired to Tailwind v4"
  key_links:
    - from: "jazlab-hub/src/app/layout.tsx"
      to: "jazlab-hub/src/app/globals.css"
      via: "import ./globals.css"
      pattern: "import.*globals\\.css"
    - from: "jazlab-hub/src/app/layout.tsx"
      to: "next/font/google"
      via: "CSS variable injection on html element"
      pattern: "font-inter.*font-space-grotesk.*font-jetbrains-mono"
    - from: "jazlab-hub/src/app/globals.css"
      to: "body::before"
      via: "noise texture pseudo-element"
      pattern: "body::before"
---

<objective>
Scaffold the JazLab Next.js 16 project and wire the complete brand design system.

Purpose: Establish the project foundation — toolchain, brand tokens, typography, dark theme, noise texture, and shadcn/ui integration — so every downstream phase builds on a fully configured brand system without revisiting setup.

Output: A running Next.js 16 app at `jazlab-hub/` with Tailwind v4 brand tokens, three-font typography, noise/grain background texture, and shadcn/ui variables bridged to JazLab colors.
</objective>

<execution_context>
@/Users/dbeacham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dbeacham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with Tailwind v4 and shadcn/ui</name>
  <files>
    jazlab-hub/package.json,
    jazlab-hub/tsconfig.json,
    jazlab-hub/next.config.ts,
    jazlab-hub/postcss.config.mjs,
    jazlab-hub/components.json,
    jazlab-hub/src/app/globals.css,
    jazlab-hub/src/app/layout.tsx,
    jazlab-hub/src/app/page.tsx,
    jazlab-hub/src/lib/utils.ts
  </files>
  <action>
    1. Run from ~/Development:
       ```
       npx create-next-app@latest jazlab-hub \
         --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
       ```
    2. cd into jazlab-hub and initialize shadcn/ui:
       ```
       npx shadcn@latest init
       ```
       Accept defaults (style: default, base color: slate, CSS variables: yes). This generates `components.json` and updates `globals.css` with `:root`/`.dark` variable blocks.

    3. Install tw-animate-css (shadcn v4 replacement for tailwindcss-animate):
       ```
       npm install tw-animate-css
       ```

    4. Verify the scaffold runs: `npm run dev` should start without errors.

    IMPORTANT: Do NOT modify globals.css or layout.tsx yet — Task 2 handles the brand system wiring. This task only scaffolds and installs.

    IMPORTANT: If `create-next-app` prompts interactively despite flags, answer: TypeScript=Yes, ESLint=Yes, Tailwind=Yes, src/=Yes, App Router=Yes, Turbopack=Yes (default), import alias=@/*.
  </action>
  <verify>
    <automated>cd /Users/dbeacham/Development/jazlab-hub && npm run dev -- --port 3099 &amp; DEV_PID=$!; sleep 8; curl -s -o /dev/null -w "%{http_code}" http://localhost:3099; RESULT=$?; kill $DEV_PID 2>/dev/null; exit $RESULT</automated>
  </verify>
  <done>jazlab-hub/ directory exists with package.json containing next, tailwindcss, and components.json from shadcn init. npm run dev starts without errors and returns HTTP 200.</done>
</task>

<task type="auto">
  <name>Task 2: Wire JazLab brand system — tokens, typography, noise, shadcn bridge</name>
  <files>
    jazlab-hub/src/app/globals.css,
    jazlab-hub/src/app/layout.tsx,
    jazlab-hub/src/app/page.tsx
  </files>
  <action>
    **globals.css — Full replacement with JazLab brand system:**

    Replace the entire contents of `src/app/globals.css` with the brand token system. The file must contain these sections in order:

    1. `@import "tailwindcss"` and `@import "tw-animate-css"`
    2. `@custom-variant dark (&:where(.dark, .dark *))` — class-based dark mode for Tailwind v4
    3. `@theme { ... }` block with ALL brand tokens:
       - Brand palette: `--color-violet: #5B3DF5`, `--color-teal: #28C7B7`, `--color-cyan: #00D4FF`, `--color-sparkle: #FFD36B`
       - Dark foundation surfaces: `--color-surface: #0F1117`, `--color-surface-raised: #1A1E2A`, `--color-surface-overlay: #242938`, `--color-border: #32384A`
       - Text scale: `--color-text-primary: #F2F4F8`, `--color-text-secondary: #A4A9B6`, `--color-text-muted: #6C7385`
       - Per-app accents: `--color-blockabye: #FF7B9C`, `--color-brickify: #FF9843`, `--color-sournal: #9B8AFB`
       - Typography: `--font-display`, `--font-body`, `--font-mono` referencing CSS variables from next/font
    4. `:root { ... }` block with shadcn/ui variables mapped to JazLab values:
       - `--background: #0F1117`, `--foreground: #F2F4F8`
       - `--card: #1A1E2A`, `--card-foreground: #F2F4F8`
       - `--primary: #5B3DF5`, `--primary-foreground: #F2F4F8`
       - `--secondary: #1A1E2A`, `--secondary-foreground: #F2F4F8`
       - `--muted: #242938`, `--muted-foreground: #A4A9B6`
       - `--accent: #28C7B7`, `--accent-foreground: #0F1117`
       - `--border: #32384A`, `--input: #32384A`, `--ring: #5B3DF5`
       - `--radius: 0.5rem`
       - Also include `--gradient-brand: linear-gradient(135deg, #5B3DF5 0%, #28C7B7 100%)` and `--gradient-accent: linear-gradient(135deg, #28C7B7 0%, #00D4FF 100%)`
    5. `@theme inline { ... }` block bridging `:root` shadcn vars to Tailwind utilities:
       - `--color-background: var(--background)`, `--color-foreground: var(--foreground)`, etc. for all shadcn variables
    6. Noise/grain texture layer (BRAND-07):
       ```css
       body {
         background-color: #0F1117;
         color: #F2F4F8;
         position: relative;
       }
       body::before {
         content: "";
         position: fixed;
         inset: 0;
         background-image: url("data:image/svg+xml,...feTurbulence fractalNoise...");
         background-repeat: repeat;
         background-size: 200px;
         opacity: 0.06;
         pointer-events: none;
         z-index: 0;
       }
       body > * {
         position: relative;
         z-index: 1;
       }
       ```
       Use the exact SVG data URI from RESEARCH.md Pattern: feTurbulence type='fractalNoise' baseFrequency='.65' numOctaves='3' stitchTiles='stitch'. Opacity 0.06 (middle of 5-8% range per user decision). pointer-events: none is CRITICAL to avoid blocking clicks.

    **layout.tsx — Three-font setup with dark class:**

    Replace contents with the root layout from RESEARCH.md Pattern 2:
    - Import `Inter`, `Space_Grotesk`, `JetBrains_Mono` from `next/font/google`
    - Each font gets a CSS variable (`--font-inter`, `--font-space-grotesk`, `--font-jetbrains-mono`)
    - `display: "swap"` on all three fonts
    - `<html lang="en" className={`dark ${inter.variable} ${spaceGrotesk.variable} ${jetbrainsMono.variable}`}>`
    - `<body className="font-body bg-surface text-text-primary antialiased">`
    - Export `metadata` with `title.default: "JazLab - A laboratory for software experiments"`, `title.template: "%s | JazLab"`, `description`, and `metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? "https://jazlab.llc")`

    IMPORTANT: Do NOT use `next-themes`. Dark class is hardcoded — JazLab is dark-only.
    IMPORTANT: Do NOT add `"use client"` to layout.tsx — it must be a Server Component.

    **page.tsx — Minimal homepage placeholder:**

    Replace with a minimal Server Component that renders a centered heading "JazLab" using `font-display text-4xl font-extrabold` and a subheading "A laboratory for software experiments" using `font-body text-text-secondary`. This confirms fonts and tokens work visually. Do NOT add `"use client"`.
  </action>
  <verify>
    <automated>cd /Users/dbeacham/Development/jazlab-hub && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - globals.css contains @theme block with all brand tokens, :root shadcn bridge, @theme inline bridge, body::before noise layer
    - layout.tsx loads three fonts via next/font, applies dark class, sets metadata with NEXT_PUBLIC_SITE_URL
    - page.tsx is a Server Component placeholder using brand token utilities
    - npm run build succeeds and homepage renders as static (○)
  </done>
</task>

</tasks>

<verification>
1. `cd jazlab-hub && npm run build` — exits 0, all pages show `○` (static), none show `λ`
2. `npm run dev` — starts without errors, page renders dark background immediately
3. Brand tokens present: globals.css contains `--color-violet`, `--color-teal`, `--color-surface`, `--font-display`
4. Noise layer present: globals.css contains `body::before` with `feTurbulence` and `opacity: 0.06`
5. shadcn bridge present: globals.css contains `@theme inline` with `--color-primary: var(--primary)`
</verification>

<success_criteria>
- jazlab-hub/ runs locally with `npm run dev`
- `npm run build` produces all static (`○`) output
- Dark background (#0F1117) renders without white flash
- All brand color utilities (bg-violet, text-teal, bg-surface-raised, etc.) are generated by Tailwind
- Three fonts load correctly (Space Grotesk for display, Inter for body, JetBrains Mono for mono)
- Noise/grain texture visible on dark background at body level only
- shadcn/ui variables wired to JazLab brand values
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
