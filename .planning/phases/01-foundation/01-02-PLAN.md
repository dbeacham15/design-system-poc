---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - jazlab-hub/src/types/experiments.ts
  - jazlab-hub/src/lib/experiments.ts
  - jazlab-hub/src/app/experiments/[slug]/page.tsx
  - jazlab-hub/vitest.config.ts
  - jazlab-hub/tests/experiments.test.ts
  - jazlab-hub/tests/brand-tokens.test.ts
  - jazlab-hub/tests/layout.test.ts
  - jazlab-hub/tests/noise-texture.test.ts
autonomous: true
requirements:
  - BRAND-04
  - BRAND-07

must_haves:
  truths:
    - "lib/experiments.ts exports a typed array with all 3 experiments (BlockAbye, Brickify, Sournal)"
    - "Each experiment has slug, name, description, status, subdomainUrl, accentColor, and features array"
    - "Experiment statuses are correct: BlockAbye=beta, Brickify=beta, Sournal=coming-soon"
    - "The [slug] route uses generateStaticParams to pre-render all 3 slugs at build time"
    - "npm run build produces all static (circle) output with zero lambda pages"
    - "All unit tests pass: experiments data, brand tokens CSS presence, layout typography, noise texture CSS rules"
  artifacts:
    - path: "jazlab-hub/src/types/experiments.ts"
      provides: "Experiment, ExperimentFeature, ExperimentStatus type definitions"
      exports: ["Experiment", "ExperimentFeature", "ExperimentStatus"]
    - path: "jazlab-hub/src/lib/experiments.ts"
      provides: "Typed experiments data array — single source of truth for all 3 apps"
      exports: ["experiments"]
    - path: "jazlab-hub/src/app/experiments/[slug]/page.tsx"
      provides: "Dynamic route with generateStaticParams for static pre-rendering"
      exports: ["generateStaticParams", "default"]
    - path: "jazlab-hub/vitest.config.ts"
      provides: "Vitest configuration with path aliases matching tsconfig"
    - path: "jazlab-hub/tests/experiments.test.ts"
      provides: "Unit tests for experiments data model completeness"
    - path: "jazlab-hub/tests/brand-tokens.test.ts"
      provides: "Unit tests for brand token CSS variable presence in globals.css"
    - path: "jazlab-hub/tests/layout.test.ts"
      provides: "Unit tests for root layout typography font variable class names"
    - path: "jazlab-hub/tests/noise-texture.test.ts"
      provides: "Unit tests for noise layer CSS rule presence and opacity"
  key_links:
    - from: "jazlab-hub/src/lib/experiments.ts"
      to: "jazlab-hub/src/types/experiments.ts"
      via: "import type { Experiment }"
      pattern: "import.*Experiment.*from.*@/types/experiments"
    - from: "jazlab-hub/src/app/experiments/[slug]/page.tsx"
      to: "jazlab-hub/src/lib/experiments.ts"
      via: "import { experiments } for generateStaticParams and page render"
      pattern: "import.*experiments.*from.*@/lib/experiments"
---

<objective>
Define the experiments data model, wire the [slug] route for static pre-rendering, and establish the test suite validating the entire Phase 1 foundation.

Purpose: Create the typed data layer that Phase 2 components and Phase 3 pages consume, ensure all routes render statically, and provide automated verification for brand tokens (BRAND-04), layout typography, and noise texture (BRAND-07).

Output: Typed experiments data in `lib/experiments.ts`, static [slug] route, Vitest configuration, and passing test suite covering data model, brand tokens, layout typography, and noise texture.
</objective>

<execution_context>
@/Users/dbeacham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dbeacham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-VALIDATION.md

<!-- Plan 01 SUMMARY needed: this plan imports from globals.css (brand tokens) to validate -->
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Types this plan creates — consumed by Phase 2 and Phase 3 -->

From src/types/experiments.ts (TO BE CREATED):
```typescript
export type ExperimentStatus = "active" | "beta" | "coming-soon";

export interface ExperimentFeature {
  title: string;
  description: string;
  icon: string;
}

export interface Experiment {
  slug: string;
  name: string;
  description: string;
  status: ExperimentStatus;
  subdomainUrl: string;
  accentColor: string;
  features: ExperimentFeature[];
}
```

From src/lib/experiments.ts (TO BE CREATED):
```typescript
export const experiments: Experiment[];
// Array of 3 items: blockabye, brickify, sournal
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create experiments type definitions and data model</name>
  <files>
    jazlab-hub/src/types/experiments.ts,
    jazlab-hub/src/lib/experiments.ts,
    jazlab-hub/src/app/experiments/[slug]/page.tsx
  </files>
  <action>
    **src/types/experiments.ts — Type definitions:**

    Create the file with exactly these exports (from RESEARCH.md Pattern 4):
    - `ExperimentStatus` type: `"active" | "beta" | "coming-soon"`
    - `ExperimentFeature` interface: `{ title: string; description: string; icon: string }`
    - `Experiment` interface: `{ slug: string; name: string; description: string; status: ExperimentStatus; subdomainUrl: string; accentColor: string; features: ExperimentFeature[] }`

    **src/lib/experiments.ts — Data array:**

    Create the file with the typed experiments array from RESEARCH.md Pattern 4. Must contain exactly 3 experiments:

    1. BlockAbye: slug="blockabye", status="beta", accentColor="#FF7B9C", subdomainUrl uses `NEXT_PUBLIC_BLOCKABYE_URL` env var with fallback "https://blockabye.jazlab.llc", 3 features (AI-generated storybooks, 6 art styles, Ready in minutes)
    2. Brickify: slug="brickify", status="beta", accentColor="#FF9843", subdomainUrl uses `NEXT_PUBLIC_BRICKIFY_URL` env var with fallback "https://brickify.jazlab.llc", 3 features (Image to LEGO, Structural validation, Step-by-step instructions)
    3. Sournal: slug="sournal", status="coming-soon", accentColor="#9B8AFB", subdomainUrl uses `NEXT_PUBLIC_SOURNAL_URL` env var with fallback "https://sournal.jazlab.llc", 3 features (Guided reflection, Private by default, Idea organization)

    Use exact descriptions and feature text from RESEARCH.md. Import type from `@/types/experiments`.

    **src/app/experiments/[slug]/page.tsx — Static [slug] route:**

    Create the directory structure `src/app/experiments/[slug]/` and the page file with:
    - `generateStaticParams()` that maps experiments to `{ slug }` objects
    - `async function ExperimentPage({ params }: { params: Promise<{ slug: string }> })` — MUST await params (Next.js 16 requirement)
    - Look up experiment by slug, call `notFound()` if not found
    - Render a minimal placeholder: experiment name as h1 (font-display), description, and status badge. This is a placeholder — Phase 3 fills in the real marketing page.
    - Do NOT add `"use client"` — must be a Server Component for static rendering

    IMPORTANT: `params` is `Promise<{ slug: string }>` in Next.js 16. Must `const { slug } = await params`. Sync access is a hard error.
  </action>
  <verify>
    <automated>cd /Users/dbeacham/Development/jazlab-hub && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - types/experiments.ts exports Experiment, ExperimentFeature, ExperimentStatus
    - lib/experiments.ts exports array of 3 experiments with all required fields
    - experiments/[slug]/page.tsx uses generateStaticParams and awaits params
    - TypeScript compiles with zero errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Set up Vitest and write Phase 1 validation tests</name>
  <files>
    jazlab-hub/vitest.config.ts,
    jazlab-hub/package.json,
    jazlab-hub/tests/experiments.test.ts,
    jazlab-hub/tests/brand-tokens.test.ts,
    jazlab-hub/tests/layout.test.ts,
    jazlab-hub/tests/noise-texture.test.ts
  </files>
  <behavior>
    - experiments.test.ts:
      - Test: experiments array has exactly 3 items
      - Test: each experiment has all required fields (slug, name, description, status, subdomainUrl, accentColor, features)
      - Test: statuses are correct (blockabye=beta, brickify=beta, sournal=coming-soon)
      - Test: each experiment has at least 1 feature with title, description, icon
      - Test: all slugs are unique
      - Test: all accentColors are valid hex strings
    - brand-tokens.test.ts:
      - Test: globals.css contains --color-violet with value #5B3DF5
      - Test: globals.css contains --color-teal with value #28C7B7
      - Test: globals.css contains --color-surface with value #0F1117
      - Test: globals.css contains --font-display, --font-body, --font-mono
      - Test: globals.css contains @theme inline block
      - Test: globals.css contains --gradient-brand custom property
    - layout.test.ts:
      - Test: layout.tsx contains the font-inter CSS variable class name
      - Test: layout.tsx contains the font-space-grotesk CSS variable class name
      - Test: layout.tsx contains the font-jetbrains-mono CSS variable class name
      - Test: layout.tsx contains the "dark" class on the html element
      - Test: layout.tsx imports from "next/font/google"
    - noise-texture.test.ts:
      - Test: globals.css contains body::before rule
      - Test: body::before contains feTurbulence
      - Test: body::before contains opacity value between 0.05 and 0.08
      - Test: body::before contains pointer-events: none
  </behavior>
  <action>
    1. Install Vitest as dev dependency:
       ```
       npm install -D vitest
       ```

    2. Create `vitest.config.ts` at project root:
       ```typescript
       import { defineConfig } from "vitest/config";
       import path from "path";

       export default defineConfig({
         test: {
           globals: true,
         },
         resolve: {
           alias: {
             "@": path.resolve(__dirname, "./src"),
           },
         },
       });
       ```

    3. Add test scripts to package.json:
       - `"test": "vitest"` (watch mode)
       - `"test:run": "vitest run"` (single run, CI-friendly)

    4. Create `tests/experiments.test.ts`:
       - Import `experiments` from `@/lib/experiments`
       - Import `Experiment` type from `@/types/experiments`
       - Write tests per the behavior spec above
       - Tests validate the data model structure, not rendering

    5. Create `tests/brand-tokens.test.ts`:
       - Read `src/app/globals.css` as a string using `fs.readFileSync`
       - Assert the CSS file contains expected token definitions
       - Check for @theme block, :root block, @theme inline block
       - Verify specific color values are present

    6. Create `tests/layout.test.ts`:
       - Read `src/app/layout.tsx` as a string using `fs.readFileSync`
       - Assert layout.tsx contains `font-inter` (the CSS variable class from next/font Inter setup)
       - Assert layout.tsx contains `font-space-grotesk` (the CSS variable class from next/font Space_Grotesk setup)
       - Assert layout.tsx contains `font-jetbrains-mono` (the CSS variable class from next/font JetBrains_Mono setup)
       - Assert layout.tsx contains the string `dark` in the html element className (hardcoded dark mode)
       - Assert layout.tsx contains `next/font/google` import
       - These are string-based file reads (same pattern as brand-tokens.test.ts), not DOM tests

    7. Create `tests/noise-texture.test.ts`:
       - Read `src/app/globals.css` as a string
       - Assert body::before rule exists with feTurbulence SVG
       - Assert opacity is in the 0.05-0.08 range (regex match)
       - Assert pointer-events: none is present

    8. Run tests: `npm run test:run` — all must pass.

    9. Run final build verification: `npm run build` — confirm all pages show `○` (static).

    IMPORTANT: Tests read CSS and TSX as strings (file system), not in a browser. This is intentional — CSS rendering and React rendering are verified manually, structure is verified automatically.
  </action>
  <verify>
    <automated>cd /Users/dbeacham/Development/jazlab-hub && npm run test:run 2>&1 | tail -15</automated>
  </verify>
  <done>
    - Vitest installed and configured with path aliases
    - tests/experiments.test.ts passes: validates 3 experiments, all fields, correct statuses
    - tests/brand-tokens.test.ts passes: validates CSS token presence
    - tests/layout.test.ts passes: validates three font variable class names and dark class in layout.tsx
    - tests/noise-texture.test.ts passes: validates noise layer rules
    - npm run test:run exits 0 with all tests green
    - npm run build exits 0 with all pages static (○)
  </done>
</task>

</tasks>

<verification>
1. `cd jazlab-hub && npm run test:run` — all tests pass (experiments, brand-tokens, layout, noise-texture)
2. `npm run build` — exits 0, all pages show `○` (static), no `λ` pages
3. `npm run build 2>&1 | grep "λ"` — returns no matches (zero dynamic pages)
4. TypeScript: `npx tsc --noEmit` — zero errors
</verification>

<success_criteria>
- lib/experiments.ts exports typed array of 3 experiments with all required fields
- types/experiments.ts exports Experiment, ExperimentFeature, ExperimentStatus
- experiments/[slug]/page.tsx uses generateStaticParams and renders statically
- Vitest configured and all test files pass (including layout.test.ts)
- npm run build produces 100% static output
- Full Phase 1 validation suite green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
